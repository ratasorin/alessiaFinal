/**
 * The **MOVE_SPEED** is the number of pixel the character moves
 * at any given input, on each axis
 * @type {number}
 */
export const MOVE_SPEED = 10;

/**
 * The **OFFSET_Y** helps setting the position of the text
 * on the Y axis
 * @type {number}
 */
export const OFFSET_Y = -20;

/**
 * The **OFFSET_X** helps setting the position of the text
 * on the X axis
 * @type {number}
 */
export const OFFSET_X = 5;

export const FRAME_DURATION = 1000 / 6;
